import {View, Text} from 'react-native';
import Strings from './consts/Strings';

const Home = () => {
    return (
        <View>
            <Text>{Strings.homeTitle}</Text>
        </View>
    )
};

export default Home;

